name: 'payments_dbt'
version: '1.0.0'


profile: 'payments_dbt'


model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  payments_dbt:
    +materialized: table   

    staging:
      +materialized: view 

    marts:
      +materialized: table  
